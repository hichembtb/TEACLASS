import 'package:flutter/material.dart';

import 'widgets/teacher_forum_body.dart';

class TeacherForumView extends StatelessWidget {
  const TeacherForumView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TeacherForumBody();
  }
}