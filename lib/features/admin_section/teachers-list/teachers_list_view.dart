import 'package:flutter/material.dart';

import 'widgets/teachers_list_body.dart';

class TeachersListView extends StatelessWidget {
  const TeachersListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TeachersListBody();
  }
}